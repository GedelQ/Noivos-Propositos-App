import PlansClient from '@/components/dashboard/plans/plans-client';

export default function PlansPage() {
  return (
    // The client component now handles its own full-width layout and styling.
    <PlansClient />
  );
}
